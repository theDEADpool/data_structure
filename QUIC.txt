QUIC(quick udp internet connctions)
优势：
1、0RTT建连
2、改进拥塞控制
3、避免行头阻塞的多路复用
4、前向纠错FEC
5、连接迁移


0RTT建连
第一次QUIC客户端连接服务器，需要1个RTT的握手，获取一些必要信息。
客户端先发一个空的hello(CHLO)，服务器会应答一个rejection(REJ)，携带客户端需要的信息，包括源地址标记和服务器证书。
客户端再次发起CHLO，就能够使用服务器整数对发送的请求进行加密。

